type Query {
    hello: String
    helloWithName(name: String): String
    customerById(id: ID): Customer
    customers: [Customer]
}

type Customer{
    id: ID
    name:String
    account: Account
}

type Account{
    id: ID
}